# 新版方向与重建边界

确认日期：2026-09-10。这里只记录用户已明确的方向与路线，具体业务规则和阶段验收由 Native change 继续确认。

## 产品方向

v域是跨行业的信息社区平台。平台提供通用能力，外部运营者经营不同业务域，内容可以使用平台提供的 Block。汽车是首个验证场景，整个项目不局限于汽车领域。

当前产品端方向是微信小程序。具体框架、数据结构和模块实现不因旧项目已使用某种方案而自动冻结。

## 重建路线

- 采用独立新版、分阶段重建。
- 先评估整个项目的重建与复用边界、阶段依赖及首期目标，再确定具体开发任务。
- 旧代码、旧测试和旧文档是参考材料；保留价值需要结合新目标核对，已有测试通过不代表新版需求已经满足。
- 旧项目保持独立。数据是否迁移、哪些数据需要保留，由整体规划明确后再执行。
- 每个真正进入实施的阶段通过 COMET Native 记录需求、完整目标规格、验收与归档。

## 已确认的体验需求

新版主题需要「浅色／深色／跟随系统」三档可选。

用户报告旧版在 Android 真机与开发者工具中出现部分页面不变色、切页闪白、弹窗或键盘颜色不一致；iOS 尚未测试。以上属于用户报告与新版验收线索，不把任何平台限制或代码根因视为已经实测确认。

主题需求不自动成为第一项业务开发任务。当前首先处理整体重建规划。

## 事实与决定的边界

- 已确认的整体方向不等于所有旧产品细则都继续生效。
- 旧项目的 Node.js、PostgreSQL、S3、登录与车源实现仅作为候选技术和复用线索。
- 新工作区不继承旧项目的活动任务、已实现能力、验收结果、密钥或连接配置。
- 后续尚未决定的第一阶段交付范围、业务域规则、数据迁移和技术选型，明确记录在 Native 规划产物中，不用默认值代替用户决定。

当前进度以 `comet.cmd status` 与 Native Runtime 为准，不在本概览维护另一套任务状态。
